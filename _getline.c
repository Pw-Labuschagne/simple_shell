#include "main.h"

/**
 * _getline - Reads inpu stream from the user
 * @argc: Ammount of characters inputted
 * @argv: Stream of string entered
 * Return: 0 on success
 */
int _getline()
{
	char str[100];

	fgets(str, sizeof(str), stdin);
	
	return (0);
}
